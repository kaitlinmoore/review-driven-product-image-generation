# Review-driven product image generation — runtime dependencies.
# Loose pins (>=) since the course environment tolerates minor variation.

# === Data pulling + preprocessing (src/) ===
requests>=2.31
python-dotenv>=1.0
openai>=1.40              # filter gate, initial prompt, structured extraction, gpt-image-1.5

# === Prompt refinement + remote image generation (src/) ===
torch>=2.1                # Mistral inference, eval-model inference, DataLoader
transformers>=4.44        # Mistral-7B-Instruct-v0.3 + CLIP/DINOv2/SigLIP processors
accelerate>=0.33          # HF infrastructure for quantized inference
bitsandbytes>=0.46.1      # 4-bit quantization for Mistral
huggingface_hub>=0.26     # InferenceClient with provider routing for FLUX.1-schnell
Pillow>=10.0              # image IO

# === Evaluation (CLIP / DINOv2 / SigLIP per REPRODUCIBILITY.md) ===
sentence-transformers>=3.0  # includes CLIP clip-ViT-B-32

# Note: `diffusers` is intentionally NOT required. The notebook prototype used
# StableDiffusionPipeline locally, but the production src/ path routes FLUX
# through fal-ai (via huggingface_hub.InferenceClient) and gpt-image through
# the OpenAI API, so no local diffusion runtime is needed.
# IPython/matplotlib/pandas appear only in notebooks that aren't on the
# production path; add if/when that code is extracted into src/.
